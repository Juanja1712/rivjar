---
interface Props {
	namePlace: string;
    description: string;
    price: String;
    imagePlace: String;
    linkPlace: String;
    
}

const { namePlace, description, price, imagePlace, linkPlace } = Astro.props
---

<div class="rounded-4xl grid justify-center items-center">
    <img class="w-64 lg:w-80 h-[80%] rounded-lg mb-4 lg:mb-8 bg-green-second" src=`${imagePlace}` alt="">
    <h1 class="mt-[3%] font-bold text-xl">{namePlace}</h1>
    <p class="text-[gray] mt-[0.625rem] w-[22rem]">{description}</p>
    <p class="font-bold mt-[5%]">{price}<span class="text-[gray]"> noche</span></p>
    <a class="flex justify-end text-[120%] text-[#224E35]" href=`${linkPlace}`><p class="hover:text-green-second mt-[5%]">Ver más</p></a>
</div>
